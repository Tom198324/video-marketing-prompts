# Environment Variables Template for Production Deployment
# Copy this file to .env and fill in your actual values

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PlanetScale MySQL connection string
# Format: mysql://username:password@host:port/database?sslaccept=strict
DATABASE_URL="mysql://user:password@aws.connect.psdb.cloud/video-marketing-prompts?sslaccept=strict"

# ============================================
# APPLICATION CONFIGURATION
# ============================================
NODE_ENV="production"
PORT=5000

# ============================================
# FRONTEND CONFIGURATION
# ============================================
# API base URL (use /api for Vercel, or full URL for separate deployments)
VITE_API_BASE_URL="/api"

# ============================================
# OAUTH CONFIGURATION (Optional)
# ============================================
# Manus OAuth credentials (if using authentication)
OAUTH_CLIENT_ID="your-manus-oauth-client-id"
OAUTH_CLIENT_SECRET="your-manus-oauth-client-secret"
OAUTH_REDIRECT_URI="https://your-app.vercel.app/api/auth/callback"
OAUTH_BASE_URL="https://auth.manus.im"

# ============================================
# LLM API CONFIGURATION (Optional)
# ============================================
# For AI variation generator feature
OPENAI_API_KEY="your-openai-api-key"
# Or use other LLM providers

# ============================================
# NOTES
# ============================================
# 1. Never commit .env file to Git (it's in .gitignore)
# 2. Add these variables to Vercel dashboard: Settings → Environment Variables
# 3. For PlanetScale, get connection string from: Database → Connect → Prisma
# 4. Update OAUTH_REDIRECT_URI after getting your Vercel URL
